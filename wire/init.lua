-- wire
-- git@github.com:italonascimento/wire.git
-- v0.0.1
-- MIT License

require 'rxlove'
local assign = 'wire.assign'
local isolate = 'wire.isolate'
local run = 'wire.run'

return {
  isolate = isolate,
  run = run,
  assign = assign
}
